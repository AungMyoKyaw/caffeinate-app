# DMG release location

Place the signed release file here as:

```text
Caffeinate.dmg
```

Alternatively, set `dmgUrl` in `../site-config.js` to the absolute URL of your hosted GitHub Release or CDN asset.

The website source archive intentionally does not contain a fabricated DMG. Publish the real macOS build before deploying the page publicly.
